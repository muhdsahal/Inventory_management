"""
Tests are intentionally disabled per user request.

Purpose:
- This file previously contained automated API tests (list/create/update/delete and validation checks).
- To avoid executing these tests during `manage.py test`, the file has been cleared and replaced with this note.

To re-enable tests:
- Restore the tests implementation here (or from the backup branch `backup-main`) and run:
    python manage.py test

"""
